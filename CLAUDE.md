# CLAUDE.md

## 文档维护规则

任何修改 `src/**` 公共 API（类型、方法签名、行为、配置项）的变更，必须在同一提交中同步更新 `docs/ai/` 下对应能力域文档的相关章节；新增公共能力须同步更新 `docs/ai/README.md` 的路由表与隐式行为清单。

`docs/ai/` 的文件名与路径是对外 API（业务项目 CLAUDE.md 直接引用其 URL），禁止重命名或移动。
